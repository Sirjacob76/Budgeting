{
  "name": "float-budget",
  "version": "1.0.0",
  "private": true,
  "description": "Float — guilt-free budgeting, packaged as a native Android app with Capacitor",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
